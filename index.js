import { TestifierMethods } from '@testifier/methods';
import { TestifierAssertions, test, expect } from '@testifier/assert';

export const testAssert = TestifierAssertions;
export const testMethods = TestifierMethods;

export * from '@testifier/interfaces';

export { test, expect }